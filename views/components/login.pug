#login-card.d-flex.justify-content-center.h-100
    .card
        .card-header
            h3 Sign In
        .card-body
            form#login-form(name='login-form')
                // username
                .input-group.form-group
                    .input-group-prepend
                        span.input-group-text
                            i.fas.fa-user
                    input.form-control(name='username' type='text' placeholder='username')
                // password
                .input-group.form-group
                    .input-group-prepend
                        span.input-group-text
                            i.fas.fa-key
                    input.form-control(name='password' type='password' placeholder='password')
                // login btn
                .form-group
                    input.btn.btn-block.login_btn(name='login' type='submit' value='Login')
                .text-danger
                    | #{message}
        .card-footer
            #sign-up-text.d-flex.justify-content-center.links
                | Don&apos;t have an account?
                a#sign-up(href='#') Sign Up
            .d-flex.justify-content-center
                a(href='#') Forgot your password?